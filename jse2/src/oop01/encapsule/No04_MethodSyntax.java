package oop01.encapsule;
/*
 * Data :
 * Author :
 * Desc : Method 문법
 */
/*
 * 메소드 개요.
 * 함수 = method = function = procedure = 처리
 * 1)메소드는 리턴값이 있어야 하는데 없으면 void라고 기술한다.
 * 2)메소드는 함수 이름 다음에 괄호()가 있어야 한다.
 * 3)메소드는 필요한 경우 메소드이름(매게인자){}를 들수있다.
 * -매게인자를 오라클에서는 parameter라고 부른다.
 * */

 /*
  * 메소드 Syntax
  * -C언어와 함수와 동일하다.
  * -데이터 처리 기능을 구현.
  * -리턴값이 없는 메소드는 void 형을 지정함.
  * -메소드가 받는 인수와 데이터 타입은 메소드를 호출하는 쪽과 일치해야함.
  * -Method Overloading(중복정의), Overriding(재정의) 기술로 확장함.
  * -메소드가 리턴하는 값과 리턴되는 값의 데이터 타입은 일치해야 함.
  */

/*
 * 메소드 종류
 * -static 메소드 : 정적메소드, 클래스 메소드
 * static이 붙은 메소드..
 * 인스턴스 변수를 사용할 수 있다.
 * 인스턴스를 사용하지 않아도 사용이 가능하다.
 * - Nos-Static메소드 : 일반메소드, 인스턴스 메소드
 * static 이 안붙어 있다.
 * 멤변 중 모든 인스턴스 공통 사용하는 메소드
 * 메소드 작업 수행시 인변을 필요로하는  메소드
 * */

/*
 * 오버로딩의 예시
 * -메소드 중복 : 메개인자 갯수나 타입만 다름
 * -메소드 이름은 동일하나 괄호안의 매개인자 갯수나 타입만 다름.
 * */
public class No04_MethodSyntax {

}
